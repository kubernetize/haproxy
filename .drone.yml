---
### Autogenerated pipeline! DONT edit! ###
kind: pipeline
name: build-arm64
node_selector:
  kubernetes.io/arch: arm64
platform:
  arch: arm64
  os: linux
  variant: v8
steps:
- image: plugins/docker
  name: build
  privileged: true
  pull: always
  settings:
    auto_tag: true
    auto_tag_suffix: arm64
    insecure: true
    registry: registry.lan
    repo: registry.lan/krichy/haproxy
  volumes:
  - name: docker
    path: /var/lib/docker
type: kubernetes
volumes:
- name: docker
  temp: {}
---
### Autogenerated pipeline! DONT edit! ###
kind: pipeline
name: build-arm
node_selector:
  kubernetes.io/arch: arm
platform:
  arch: arm
  os: linux
  variant: v7
steps:
- image: plugins/docker
  name: build
  privileged: true
  pull: always
  settings:
    auto_tag: true
    auto_tag_suffix: arm
    insecure: true
    registry: registry.lan
    repo: registry.lan/krichy/haproxy
  volumes:
  - name: docker
    path: /var/lib/docker
type: kubernetes
volumes:
- name: docker
  temp: {}
---
depends_on:
- build-arm64
- build-arm
kind: pipeline
name: manifest
platform:
  arch: arm64
  os: linux
  variant: v8
steps:
- image: plugins/manifest
  name: registry.lan/krichy/haproxy
  pull: always
  settings:
    auto_tag: true
    insecure: true
    registry: registry.lan
    spec: manifests/registry.lan-krichy-haproxy.tmpl
type: kubernetes
